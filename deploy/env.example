# Copy to /home/pi/english-cockpit/.env (create once). Empty values disable a feature.
LLM_API_KEY=
DEEPGRAM_API_KEY=

# Optional tuning (uncomment to override defaults):
# LLM_DAILY_LIMIT=1000
# DEEPGRAM_DAILY_LIMIT=200
# RATE_LIMIT_PER_MINUTE=30
